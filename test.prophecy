#!./execProphecyCompilerFromSource.sh

val array = [11, 22, 33]

array.set(1, 42)

System.println('size:')
System.println(array.size())

System.println('values:')

foreach i in Array.range(0, array.size())
    System.println(array.get(i))
